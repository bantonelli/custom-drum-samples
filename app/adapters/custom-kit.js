import DRFAdapter from './drf';

export default DRFAdapter.extend({
    // buildURL: function(root, suffix, record) {
    // }
 //    buildURL: function(type, id, record) {
	//     var url = this._super();
	//     return url + 'profiles/' + id + '/';
	// }
});