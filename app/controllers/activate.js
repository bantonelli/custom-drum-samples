import Ember from 'ember';

export default Ember.Controller.extend({
	// The controller will have the email and password credentials available via the router ajax call
		// Server should send back credentials as json response.
		// Make this a Login mixin controller that calls authenticate method using credentials.
			// This will allow the user to be logged in automatically after activation.
});
